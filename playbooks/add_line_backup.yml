---

- name: Añade línea al backup 
  hosts: all
  become: yes
  become_user: root
  vars:
      - tmpfile: "/tmp/output"
  tasks:

    - name: Insertar linea
      lineinfile:
          path: /etc/backup/include
          line: /srv/websites
      register: out


